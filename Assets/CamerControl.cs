using UnityEngine;
using System.Collections;

public class CamerControl : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        var offsetX = Input.GetAxis("Horizontal");
        var offsetY = Input.GetAxis("Vertical");

	}
}
